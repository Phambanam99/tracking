# Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        MapComponent.tsx                          │
│                                                                  │
│  const mapRef = useRef<HTMLDivElement>(null);                   │
│  const mapInstanceRef = useRef<Map>(null);                      │
│                                                                  │
│  useMapInitialization({                                         │
│    mapRef, mapInstanceRef, aircraftLayerRef, vesselLayerRef    │
│  });                                                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              useMapInitialization (Orchestrator)                 │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Create base map layer                                 │  │
│  │ 2. Initialize icon cache                                 │  │
│  │ 3. Create vehicle configs (aircraft, vessel)             │  │
│  │ 4. Create style factories                                │  │
│  │ 5. Create plugins                                        │  │
│  │ 6. Initialize OpenLayers Map                             │  │
│  │ 7. Setup zoom handlers                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└───┬──────────────┬──────────────┬──────────────┬───────────────┘
    │              │              │              │
    ▼              ▼              ▼              ▼
┌────────┐  ┌────────────┐  ┌───────────┐  ┌─────────────┐
│ Base   │  │  Icon      │  │  Style    │  │   Plugin    │
│ Map    │  │  Cache     │  │  Factory  │  │   System    │
│ Layer  │  │  System    │  │           │  │             │
└────────┘  └────────────┘  └───────────┘  └─────────────┘
```

## 📦 Module Dependencies

```
types.ts (Core Types)
    │
    ├─► cache.ts (LRU Cache)
    │       │
    │       └─► useIconCache.ts
    │               │
    │               └─► useVehicleStyleFactory.ts
    │                           │
    ├───────────────────────────┴─► VehicleLayerPlugin.ts
    │                                       │
    └─► vehicleConfigFactory.ts ───────────┤
            │                               │
            └─► useBaseMapLayer.ts ◄────────┤
                        │                   │
                        └─► useMapInitialization.refactored.ts
                                    │
                                    └─► index.ts (Public API)
```

## 🔄 Data Flow

### Icon Rendering Pipeline

```
┌──────────────┐
│ Load SVG     │
│ ./icon.svg   │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│  Icon Cache     │
│  - Image loaded │
│  - Promise API  │
└──────┬──────────┘
       │
       ▼
┌─────────────────────────┐
│  Color Tinting          │
│  - Canvas manipulation  │
│  - Apply color          │
│  - Cache result         │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────┐
│  Style Creation     │
│  - Icon + rotation  │
│  - Cache in LRU     │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Render on Map      │
│  - OpenLayers       │
└─────────────────────┘
```

### Cluster Rendering Pipeline

```
┌──────────────────────┐
│  Features (n items)  │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Cluster Algorithm   │
│  - Group by distance │
└──────┬───────────────┘
       │
       ├─► Single item ──┐
       │                 │
       └─► Cluster (n>1) │
                         │
    ┌────────────────────┘
    │
    ▼
┌─────────────────────────┐
│  Style Decision         │
│  - Zoom level           │
│  - Item count           │
└──────┬──────────────────┘
       │
       ├─► Low zoom ──► Dot style
       │
       └─► High zoom ──► Icon style
                         │
                         ▼
                    ┌─────────────┐
                    │  LRU Cache  │
                    │  98% hit    │
                    └─────────────┘
```

## 🎯 Plugin Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    IMapLayerPlugin                          │
│  (Interface)                                               │
│                                                            │
│  + id: string                                              │
│  + name: string                                            │
│  + initialize(map: Map): void                              │
│  + createLayer(): VectorLayer                              │
│  + getSource(): VectorSource                               │
│  + setVisible(visible: boolean): void                      │
│  + destroy(): void                                         │
└─────────────────┬──────────────────────────────────────────┘
                  │
                  │ implements
                  │
┌─────────────────┴──────────────────────────────────────────┐
│              VehicleLayerPlugin                             │
│  (Base Implementation)                                     │
│                                                            │
│  - config: VehicleTypeConfig                               │
│  - styleFactory: StyleFactory                              │
│  - source: VectorSource                                    │
│  - clusterSource?: Cluster                                 │
│  - layer: VectorLayer                                      │
│                                                            │
│  + createStyleFunction(): StyleFunction                    │
│  + updateClusterDistance(dist: number): void               │
└─────────────────┬──────────────────────────────────────────┘
                  │
                  │ instantiated by
                  │
┌─────────────────┴──────────────────────────────────────────┐
│             VehicleLayerFactory                             │
│  (Factory)                                                 │
│                                                            │
│  static createPlugin(                                      │
│    config: VehicleTypeConfig,                              │
│    styleFactory: StyleFactory                              │
│  ): VehicleLayerPlugin                                     │
└────────────────────────────────────────────────────────────┘
```

### Extensibility Example

```
Want to add Drone layer?

1. Create Config:
   ┌────────────────────────┐
   │ createDroneConfig()    │
   │ - type: 'drone'        │
   │ - icon: drone-icon.svg │
   │ - color: purple        │
   └────────────────────────┘
           │
           ▼
2. Create Plugin:
   ┌────────────────────────────┐
   │ VehicleLayerFactory        │
   │   .createPlugin(           │
   │     droneConfig,           │
   │     droneStyleFactory      │
   │   )                        │
   └────────────────────────────┘
           │
           ▼
3. Add to Map:
   ┌────────────────────────────┐
   │ const layer =              │
   │   dronePlugin.createLayer()│
   │ map.addLayer(layer)        │
   └────────────────────────────┘

Done! No changes to core system.
```

## 🔧 Cache System

```
┌───────────────────────────────────────────────────────────┐
│                     LRU Cache                              │
│                                                            │
│  Internal Map:                                             │
│  ┌──────────┬────────────────┬─────────┬──────────────┐   │
│  │   Key    │     Value      │ LastUsed│ UsageCount   │   │
│  ├──────────┼────────────────┼─────────┼──────────────┤   │
│  │ "0|blue" │ Style{...}     │ 1699... │      42      │   │
│  │ "15|red" │ Style{...}     │ 1699... │      38      │   │
│  │ "30|blue"│ Style{...}     │ 1699... │      156     │ ◄─┐
│  │ ...      │ ...            │ ...     │      ...     │   │
│  └──────────┴────────────────┴─────────┴──────────────┘   │
│                                                            │
│  Operations:                                               │
│  ┌────────────────────────────────────────────┐           │
│  │ get(key): T | undefined                    │           │
│  │   - Update lastUsed, usageCount            │           │
│  │   - Check TTL                              │           │
│  │   - Return cached value                    │           │
│  └────────────────────────────────────────────┘           │
│                                                            │
│  ┌────────────────────────────────────────────┐           │
│  │ set(key, value): void                      │           │
│  │   - Check capacity                         │           │
│  │   - Evict LRU if full                      │   Cache   │
│  │   - Store new entry                        │    Hit!   │
│  └────────────────────────────────────────────┘           │
│                                                            │
│  ┌────────────────────────────────────────────┐           │
│  │ evictLRU(): void                           │           │
│  │   - Find entry with oldest lastUsed        │           │
│  │   - Remove from cache                      │           │
│  └────────────────────────────────────────────┘           │
│                                                            │
│  Stats: { size, maxSize, totalUsage, avgUsage }           │
└───────────────────────────────────────────────────────────┘
```

## 🎨 Style Creation Flow

```
Feature on Map
    │
    ▼
┌─────────────────────┐
│ Style Function      │
│ (feature, res) =>   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Extract Data        │
│ - heading           │
│ - operator/flag     │
│ - cluster size      │
└──────┬──────────────┘
       │
       ├─► Is Cluster?
       │      │
       │      ├─► Yes (n>1) ──► Create Cluster Style
       │      │                      │
       │      │                      └─► Check Cache ──► Return
       │      │
       │      └─► No (n=1)
       │             │
       │             ▼
       │      Low Zoom?
       │             │
       │             ├─► Yes ──► Create Dot Style
       │             │              │
       │             │              └─► Check Cache ──► Return
       │             │
       │             └─► No
       │                  │
       │                  ▼
       │           Create Icon Style
       │                  │
       │                  ├─► Quantize heading (0°,15°,30°...)
       │                  │
       │                  ├─► Get color from config
       │                  │
       │                  ├─► Create cache key
       │                  │
       │                  ├─► Check cache (98% hit!)
       │                  │      │
       │                  │      ├─► Hit ──► Return cached
       │                  │      │
       │                  │      └─► Miss
       │                  │           │
       │                  │           ├─► Get base image
       │                  │           │
       │                  │           ├─► Tint with color
       │                  │           │
       │                  │           ├─► Create style
       │                  │           │
       │                  │           └─► Cache & return
       │                  │
       └──────────────────┴─► Style applied to feature
```

## 📊 Memory Management

```
Time ─────────────────────────────────────────────────►

Old System:
Memory │     ╱╲      ╱╲
(MB)   │    ╱  ╲    ╱  ╲    ╱╲
400    │   ╱    ╲  ╱    ╲  ╱  ╲
       │  ╱      ╲╱      ╲╱
200    │╱╱
       │
       └────────────────────────────
          Unbounded growth
          No cleanup
          Memory leaks

New System:
Memory │
(MB)   │ ┌────────────────────────┐
100    │ │    LRU Cache           │
       │ │    Max Size: 500       │
80     │ │    Auto-eviction       │
       │ │    TTL cleanup         │
       │ └────────────────────────┘
       │
       └────────────────────────────
          Stable footprint
          Predictable memory
          No leaks
```

## 🔌 Extensibility Points

```
┌─────────────────────────────────────────────────────────────┐
│                    Extension Points                          │
│                                                              │
│  1. Vehicle Types                                            │
│     ┌────────────────────────────────────────┐              │
│     │ - Aircraft ✓                           │              │
│     │ - Vessel ✓                             │              │
│     │ - Drone (ready to add)                 │              │
│     │ - Satellite (ready to add)             │              │
│     │ - Submarine (future)                   │              │
│     │ - UAV (future)                         │              │
│     └────────────────────────────────────────┘              │
│                                                              │
│  2. Map Providers                                            │
│     ┌────────────────────────────────────────┐              │
│     │ - OpenStreetMap ✓                      │              │
│     │ - MapTiler ✓                           │              │
│     │ - OpenSeaMap ✓                         │              │
│     │ - Custom sources ✓                     │              │
│     │ - Google Maps (can add)                │              │
│     │ - Bing Maps (can add)                  │              │
│     └────────────────────────────────────────┘              │
│                                                              │
│  3. Style Strategies                                         │
│     ┌────────────────────────────────────────┐              │
│     │ - Icon-based ✓                         │              │
│     │ - Dot-based ✓                          │              │
│     │ - Cluster-based ✓                      │              │
│     │ - Heatmap (can add)                    │              │
│     │ - 3D extrusion (can add)               │              │
│     └────────────────────────────────────────┘              │
│                                                              │
│  4. Cache Strategies                                         │
│     ┌────────────────────────────────────────┐              │
│     │ - LRU ✓                                │              │
│     │ - LFU (can switch)                     │              │
│     │ - TTL-based ✓                          │              │
│     │ - Size-based ✓                         │              │
│     └────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────┘
```

---

**Legend:**

- `✓` = Implemented
- `(ready to add)` = Interface ready, needs implementation
- `(can add)` = Can be added with current architecture
- `(future)` = Planned for future releases
